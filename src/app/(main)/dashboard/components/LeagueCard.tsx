"use client";

/**
 * Compatibility shell only.
 *
 * League/leaderboard competition is outside the active product scope. Keeping
 * this component temporarily avoids a risky edit to the large dashboard
 * orchestrator while removing all league UI, network calls and stale links.
 */
export default function LeagueCard() {
  return null;
}
