# Agent Plan — Current Work Only

> This file describes current cleanup work only. Historical work belongs in Git commits and pull requests.

## Current task

| Field | Value |
|---|---|
| Task | CLEANUP-006 — Dependency classification |
| Status | done — awaiting stacked PR review |
| Branch | `agent/cleanup-dependency-classification` |
| Goal | Remove proven dependency waste, move build tooling out of runtime dependencies, and document verified false positives |

## Completed in earlier cleanup batches

- Stopped automatic maintenance-task generation, commits, and direct pushes.
- Added reproducible source and dependency inventory plus durable cleanup evidence.
- Reduced conservative unreachable source candidates from 17 to 0.
- Added focused Supabase session regression coverage while removing the obsolete middleware helper.
- Reconciled protected-route E2E coverage with intentional guest self-study behavior.

## Completed in CLEANUP-006

Removed:

- `@types/recharts`
- `tw-animate-css`

Moved from runtime dependencies to development dependencies:

- `@tailwindcss/postcss`
- `shadcn`

Verified and retained as configuration, framework, CI, or type-system dependencies:

- `lint-staged`
- `postcss`
- `react-dom`
- `typescript`
- `wait-on`
- `@types/node`
- `@types/react-dom`
- development-only `@tailwindcss/postcss`
- development-only `shadcn`

Evidence is recorded in `reports/dependency-classification.md`.

## Validation completed

The package graph was generated by npm and validated before commit. A second clean-state run confirmed the committed manifest and lockfile regenerate with zero diff.

```bash
npm install --package-lock-only --ignore-scripts --legacy-peer-deps
git diff --exit-code -- package.json package-lock.json
npm ci --ignore-scripts --legacy-peer-deps
npx shadcn --version
node -e "require.resolve('@tailwindcss/postcss')"
npm run inventory -- --write
npx tsc --noEmit
npm run lint
npm run test
npm run test:content-standard
npm run build
```

Results:

- lockfile stability passed
- clean install passed
- retained CLI/build tooling passed
- TypeScript passed
- ESLint passed
- full unit tests passed
- lesson content-standard tests passed
- production build passed
- no dependency version changed

The temporary validation workflow was removed after the final successful run.

## Next action

CLEANUP-014 — add focused behavior coverage around `UnitTemplate` before the first reversible extraction. Do not change completion transactions, database actions, curriculum data, or lesson ordering in that test-foundation batch.
