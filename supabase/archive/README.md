# Archived SQL (do not apply)

These files are **historical snapshots** from manual Supabase SQL Editor runs before the migrations folder was canonical.

Use `supabase/migrations/` + `supabase/README.md` instead.

Files kept for reference only:
- `schema.sql`, `schema_v5`–`v7` — incremental manual schema versions
- `migration_v2.sql` — cards/user_progress cleanup script
- `migration_v8.sql` — superseded by `20260621150000_daily_xp_goal.sql`
- `rls_policies.sql`, `security_hardening.sql` — one-off RLS patches