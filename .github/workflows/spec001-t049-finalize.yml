name: Spec 001 T049 Finalize

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read

jobs:
  prepare:
    if: github.head_ref == 'agent/rebuild-learning-core'
    runs-on: ubuntu-latest
    steps:
      - name: Confirm finalizer availability
        run: echo "T049 finalizer is ready"
