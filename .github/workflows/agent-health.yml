name: Agent Health (manual)

on:
  workflow_dispatch:

jobs:
  health:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: grep -c '\*\*Status:\*\* `ready`' AGENT_BACKLOG.md || true