name: Spec 001 Status Note

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read

jobs:
  note:
    if: github.head_ref == 'agent/rebuild-learning-core'
    runs-on: ubuntu-latest
    steps:
      - run: echo "Spec 001 status note prepared"
